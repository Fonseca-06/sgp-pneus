{
  "name": "sgp-pneus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sistema de gestão de clientes, preços e pedidos de pneus",
  "scripts": {
    "build": "node scripts/verificar-build.mjs",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "gates": "npm run build && npm run lint && npm test",
    "dev": "python3 server.py"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "globals": "^16.5.0",
    "jsdom": "^28.0.0",
    "vitest": "^4.1.10"
  }
}
